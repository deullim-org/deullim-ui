// Distance radius (in meters) used to decide which nearby memos trigger a push.
export const MIN_RADIUS_M = 50;
export const MAX_RADIUS_M = 2000;
export const DEFAULT_RADIUS_M = 500;
export const RADIUS_STEP_M = 50;
